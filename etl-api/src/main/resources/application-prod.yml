# ── Prod profile ──
spring:
  datasource:
    url: jdbc:postgresql://${DB_HOST:localhost}:5432/etldb_prod
    driver-class-name: org.postgresql.Driver
    username: ${DB_USER}
    password: ${DB_PASS}

logging:
  level:
    com.generic.etl: WARN
    org.apache.camel: ERROR

etl:
  retry:
    max-retries: 3
  api-keys: ${ETL_API_KEYS}

server:
  shutdown: graceful
  tomcat:
    max-connections: 200
    threads:
      max: 50

eureka:
  client:
    enabled: true
    service-url:
      defaultZone: http://eureka-prod:8761/eureka/
